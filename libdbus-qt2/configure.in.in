AC_INIT(acinclude.m4)
AC_CONFIG_AUX_DIR(admin)

AM_INIT_AUTOMAKE(dbus-1-qt3, 0.9)

KDE_PROG_LIBTOOL

AC_PATH_QT
AC_PATH_QT_MOC_UIC

LIBS="$LIBS $X_LDFLAGS"

AC_CONFIG_FILES([dbus-1-qt3.pc])

########### Check for DBus

KDE_PKG_CHECK_MODULES( DBUS, "dbus-1", [ DBUS_INCS=$DBUS_CFLAGS; ] , AC_MSG_ERROR([D-Bus could not be found]) )

AC_SUBST(DBUS_INCS)
AC_SUBST(DBUS_LIBS)

