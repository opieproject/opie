OpiePIM.kpr A KPresenter comparsion of Qtopia, KDE and OPIE PIM
OpiePIM.tar.gz Same as above but webpages
toolchain.tar.gz  Opie needs a toolchain to be built on x86
		  but Opie also needs some additional headers
		  and libraries to be built
		  This archive contains
		  libuuid lib + header
		  pam lib + header
		  
		  -zecke