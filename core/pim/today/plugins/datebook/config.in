  config TODAY_DATEBOOK
    boolean "opie-today-datebookplugin"
    default "y"
    depends ( LIBQPE || LIBQPE-X11 ) && LIBOPIE2UI && LIBOPIE2CORE && LIBOPIE2PIM

    comment "opie-today-datebookplugin requires libopie2ui and libopie2core"
    depends !( LIBQPE || LIBQPE-X11 ) && LIBOPIE2UI && LIBOPIE2CORE && LIBOPIE2PIM

