After running qdoc in the directory above, the html directory created here should
be renamed to "qtopia" and moved to $QTDIR/doc/html/

Don't forget to:
cp $QPEDIR/doc/images/* $QPEDIR/doc/html
before running qdoc...
